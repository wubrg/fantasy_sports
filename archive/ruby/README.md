# Archived: Ruby

`.ruby-version`, `gemfile`, `gemfile.lock` — leftover Ruby tooling stub from
an earlier point in the repo's history. No `.rb` source files ever
accompanied it, and nothing in the current Go apps (`league_home/`,
`canton/`) depends on it. Kept here for reference rather than deleted.
