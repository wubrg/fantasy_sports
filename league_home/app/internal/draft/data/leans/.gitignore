# Generated lean sets are derived from the private source repo, so they stay
# out of this public one. `draftroom leans -generate` rebuilds them locally.
*.csv
*.yaml
*.yml
!mine.yaml
# blank.yaml is a generic empty set (no reads), not derived from private
# source data, so it is tracked — the research boards point `-leans` at it.
!blank.yaml
